import { Modal } from './ui/Modal.js';

interface HelpModalProps {
  isOpen: boolean;
  onClose: () => void;
}

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div className="flex flex-col gap-6">
      <div
        className="text-xs font-medium px-2 py-1"
        style={{
          color: 'var(--color-accent-bright)',
          borderBottom: '1px solid var(--color-border)',
          paddingBottom: 6,
        }}
      >
        {title}
      </div>
      <div className="flex flex-col gap-4 px-2">{children}</div>
    </div>
  );
}

function Row({ label, desc }: { label: string; desc: string }) {
  return (
    <div className="flex gap-8 items-start">
      <span
        className="text-xs shrink-0"
        style={{
          minWidth: 130,
          color: 'var(--color-text)',
          background: 'var(--color-btn-bg)',
          border: '1px solid var(--color-border)',
          padding: '2px 6px',
        }}
      >
        {label}
      </span>
      <span className="text-xs" style={{ color: 'var(--color-text-muted)', lineHeight: 1.6 }}>
        {desc}
      </span>
    </div>
  );
}

export function HelpModal({ isOpen, onClose }: HelpModalProps) {
  return (
    <Modal
      isOpen={isOpen}
      onClose={onClose}
      title="Справка"
      className="min-w-[480px] max-w-[600px]"
    >
      <div
        className="px-10 pb-10 flex flex-col gap-12"
        style={{ maxHeight: '70vh', overflowY: 'auto' }}
      >
        <Section title="НИЖНЯЯ ПАНЕЛЬ">
          <Row
            label="+ Агент"
            desc="Открывает диалог запуска нового агента Claude. Можно выбрать скин персонажа, написать начальный промпт, прикрепить шаблон или включить режим без разрешений."
          />
          <Row
            label="⚠ (оранжевый)"
            desc="Режим без запросов разрешений. Когда включён — агенты выполняют любые команды без подтверждения. Используй осторожно."
          />
          <Row
            label="Макет"
            desc="Включает редактор офиса. В режиме редактирования можно рисовать полы, стены, расставлять мебель, изменять размер офиса."
          />
          <Row
            label="Настройки"
            desc="Открывает панель настроек: импорт/экспорт макета, звук, хуки, папки ресурсов и язык интерфейса."
          />
          <Row label="? (эта кнопка)" desc="Открывает эту справку." />
          <Row
            label="● (цветная точка)"
            desc="Статус хук-сервера. Зелёный — хуки подключены (мгновенное обнаружение). Жёлтый — переподключение. Красный — отключены, работает медленный режим."
          />
        </Section>

        <Section title="ПЕРСОНАЖИ">
          <Row
            label="Клик"
            desc="Выделяет персонажа. Камера плавно следует за ним. Повторный клик — снимает выделение."
          />
          <Row
            label="Двойной клик"
            desc="Открывает настройку персонажа: смена скина, имени и режима отдыха."
          />
          <Row
            label="Правый клик"
            desc="Контекстное меню: переименовать, сменить скин, закрыть агента."
          />
          <Row
            label="Клик по месту"
            desc="После выбора персонажа — клик по свободному стулу пересаживает его туда."
          />
          <Row
            label="Пузырь ..."
            desc="Агент ожидает подтверждения разрешения. Нажми на него чтобы переключиться в терминал."
          />
          <Row label="Пузырь ✓" desc="Агент завершил задачу и ждёт следующей команды." />
        </Section>

        <Section title="НАСТРОЙКА ПЕРСОНАЖА">
          <Row
            label="Персонаж"
            desc="Выбери скин из доступных. Кастомные персонажи из внешней папки появляются здесь автоматически."
          />
          <Row label="Имя" desc="Необязательное имя, отображается над персонажем в офисе." />
          <Row
            label="Отдых"
            desc="Что делает персонаж когда не работает: пьёт кофе ☕, спит 💤, смотрит в телефон 📱 или случайно."
          />
        </Section>

        <Section title="РЕДАКТОР МАКЕТА (кнопка Макет)">
          <Row
            label="Пол (кисть)"
            desc="Рисует плитку пола. Правый клик — стирает до пустоты. Клик по пустому краю офиса — расширяет сетку."
          />
          <Row
            label="Стена"
            desc="Добавляет стены. Клик по существующей стене — удаляет. Стены автоматически соединяются с соседними."
          />
          <Row
            label="Мебель"
            desc="Выбери предмет из каталога справа и кликни на плитку чтобы поставить. R — повернуть, T — вкл/выкл."
          />
          <Row label="Пипетка" desc="Клик по мебели — копирует её тип для следующего размещения." />
          <Row
            label="Выделение"
            desc="Клик по предмету — выделяет. Можно перетащить, удалить (красный ×) или покрасить через HSBC-ползунки."
          />
          <Row
            label="Ластик"
            desc="Стирает плитки в пустоту. Правый клик тоже стирает в любом режиме."
          />
          <Row label="Ctrl+Z / Ctrl+Y" desc="Отменить / повторить действие. До 50 шагов истории." />
          <Row
            label="Сохранить"
            desc="Сохраняет макет в ~/.ai-pixel-office/layout.json. Макет общий для всех окон VS Code."
          />
        </Section>

        <Section title="НАСТРОЙКИ">
          <Row
            label="Экспорт макета"
            desc="Сохраняет текущий офис в JSON-файл — для бэкапа или передачи другому пользователю."
          />
          <Row
            label="Импорт макета"
            desc="Загружает JSON-файл с макетом (формат version:1). Заменяет текущий офис."
          />
          <Row
            label="Добавить папку ресурсов"
            desc="Указывает папку с кастомными персонажами и мебелью. Структура: папка/assets/characters/ и папка/assets/furniture/."
          />
          <Row
            label="Звук"
            desc="Включает звуковой сигнал когда агент завершил работу и ждёт команды."
          />
          <Row
            label="Отслеживать все сессии"
            desc="Автоматически подхватывает Claude-сессии запущенные вне расширения (из терминала напрямую)."
          />
          <Row
            label="Мгновенное обнаружение"
            desc="Использует хук-систему Claude Code для мгновенного отслеживания событий. Рекомендуется держать включённым."
          />
          <Row
            label="Всегда показывать метки"
            desc="Показывает имена агентов над персонажами постоянно, а не только при наведении."
          />
          <Row
            label="Без запросов разрешений"
            desc="То же что кнопка ⚠ в тулбаре — глобальное отключение запросов для всех новых агентов."
          />
        </Section>

        <Section title="ДОБАВЛЕНИЕ ПЕРСОНАЖЕЙ">
          <Row
            label="Формат"
            desc="PNG 112×96 пикселей: 7 кадров по горизонтали × 3 ряда направлений (вниз / вверх / вправо). Каждый кадр 16×32 px."
          />
          <Row
            label="Кадры (по порядку)"
            desc="0-ходьба1, 1-стоит, 2-ходьба2, 3-печатает1, 4-печатает2, 5-читает1, 6-читает2."
          />
          <Row
            label="Куда класть"
            desc="В папку указанную через «Добавить папку ресурсов», внутрь: assets/characters/char_N.png (N = 6, 7, 8...)."
          />
          <Row
            label="Конвертер"
            desc="Если у тебя спрайт-лист в другом формате — используй: npm run convert-character -- <source.png> <output.png>"
          />
        </Section>
      </div>
    </Modal>
  );
}
